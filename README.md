# Pipette.NET
This repository handles the dotnet tool that searches for the Testing Vial in a .NET project and generates a .vial file

## Usage

`pipette (--project <path-to-csproj> | --solution <path-to-sln>) [--vial-version <version>]`
